# React 19 peer-dependency conflicts (react-helmet-async and friends have not
# updated their peer ranges). Previously passed as --legacy-peer-deps in
# apps/web/Dockerfile; under a single workspace install it has to be repo-level.
legacy-peer-deps=true

# Fail fast rather than silently resolving a different tree than the lockfile.
engine-strict=false
