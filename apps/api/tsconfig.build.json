{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "incremental": false
  },
  "include": [
    "**/*.ts"
  ],
  "exclude": [
    "node_modules",
    "dist",
    "coverage",
    "uploads",
    "tests",
    "**/*.test.ts",
    "**/*.spec.ts"
  ],
  "//": "Emits the production build. Excludes the test suite: tests/ and *.test.ts were already excluded, but *.spec.ts (86 files colocated in lib/, controllers/ and prisma/) were being emitted into dist/. Comments are deliberately KEPT: lib/swaggerConfig.ts feeds the emitted files to swagger-jsdoc, which reads the hand-written @swagger JSDoc blocks out of them."
}
